{
  "name": "voxel-aeronautics-workshop",
  "version": "0.4.0-foundation.1c2",
  "private": true,
  "description": "Voxel airship engineering sandbox — oriented craft control frames, six-axis input profiles, and persistent resizable workspace panels.",
  "scripts": {
    "test": "python tests/run_all.py",
    "build": "python tools/build_release.py",
    "serve": "python tools/serve.py",
    "verify-release": "python tools/verify_release.py"
  },
  "engines": {
    "node": ">=18",
    "python": ">=3.10"
  }
}
