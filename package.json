{
  "name": "voxel-aeronautics-workshop",
  "version": "0.6.0-foundation.1d3b",
  "private": true,
  "description": "Desktop voxel airship engineering sandbox — rebindable input, persistent block identities, production assembly building, automated real-Cannon parity, deterministic headless physics, strict runtime contracts, guided controls, and multi-pad missions.",
  "scripts": {
    "test": "python tests/run_all.py",
    "build": "python tools/build_release.py",
    "serve": "python tools/serve.py",
    "verify-release": "python tools/verify_release.py"
  },
  "engines": {
    "node": ">=18",
    "python": ">=3.10"
  }
}
