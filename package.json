{
  "name": "voxel-aeronautics-workshop",
  "version": "0.6.2-foundation.1d3c",
  "private": true,
  "description": "Desktop voxel airship engineering sandbox — rebindable input, persistent block identities, production assembly building, automated real-Cannon parity, deterministic headless physics, strict runtime contracts, an explicitly composed modular game shell, a minimal hinge-only Physics Port constraint contract, real-Cannon motor/servo validation, guided controls, and multi-pad missions.",
  "scripts": {
    "test": "python tests/run_all.py",
    "build": "python tools/build_release.py",
    "serve": "python tools/serve.py",
    "verify-release": "python tools/verify_release.py"
  },
  "engines": {
    "node": ">=18",
    "python": ">=3.10"
  }
}
