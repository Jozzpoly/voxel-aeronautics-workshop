{
  "name": "voxel-aeronautics-workshop",
  "version": "0.7.0-foundation.1d4a",
  "private": true,
  "description": "Desktop voxel engineering sandbox with Blueprint v11 mechanical links, deterministic rigid-island compilation, per-body coordinate spaces, RuntimeAssemblyPlan V2, and a normal articulated-flight path.",
  "scripts": {
    "test": "python tests/run_all.py",
    "build": "python tools/build_release.py",
    "serve": "python tools/serve.py",
    "verify-release": "python tools/verify_release.py"
  },
  "engines": {
    "node": ">=18",
    "python": ">=3.10"
  }
}
