{
  "name": "voxel-aeronautics-workshop",
  "version": "0.5.9-foundation.1d3a",
  "private": true,
  "description": "Desktop voxel airship engineering sandbox — rebindable input, persistent block identities, production assembly building, deterministic headless physics, mass-property parity, guided controls, and multi-pad missions.",
  "scripts": {
    "test": "python tests/run_all.py",
    "build": "python tools/build_release.py",
    "serve": "python tools/serve.py",
    "verify-release": "python tools/verify_release.py"
  },
  "engines": {
    "node": ">=18",
    "python": ">=3.10"
  }
}
