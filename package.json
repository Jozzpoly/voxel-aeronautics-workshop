{
  "name": "voxel-aeronautics-workshop",
  "version": "0.6.4-foundation.1d3e",
  "private": true,
  "description": "Desktop voxel engineering sandbox with RuntimeAssembly-owned flight lifecycle, exact per-body integrity ownership, per-body visuals, retry-safe cleanup, real-Cannon hinge validation, deterministic headless physics, and a modular desktop game shell.",
  "scripts": {
    "test": "python tests/run_all.py",
    "build": "python tools/build_release.py",
    "serve": "python tools/serve.py",
    "verify-release": "python tools/verify_release.py"
  },
  "engines": {
    "node": ">=18",
    "python": ">=3.10"
  }
}
